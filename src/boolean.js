/* eslint-disable*/
// boolean
// = sign used for assignment/ updation of values
//=== checks both type & Value
// == checks value
let isDrawing = false;
let age = 18;
const ofAge=age>19;
console.log(ofAge);

const s1='18';
console.log(s1==18);

10==='10'// false as values are same but types are different
10=='10' // true as values are same